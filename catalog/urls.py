from django.urls import path
from . import views

# urlpatterns = [
#     path('catalog/', views.BookListView.as_view(), name='book-list-view'),
#     path('catalog/<id>/', views.BookDetailView.as_view, name='book-detail-view')
# ]